version: 0.0.13
container_uri: docker://ghcr.io/blockjoy/base-op-reth:v20250117.39
sku_code: BASE-OP-RETH
org_id: null
description: Base OP Reth node
visibility: public
protocol_key: base
variants:
  - key: base-op-reth-mainnet-full
    min_cpu: 32
    min_memory_mb: 32678
    min_disk_gb: 2000
    sku_code: BASE-OPRETH-MF
    archive_pointers:
      - pointer: !store_key base-op-reth-mainnet-full-v1
  - key: base-op-reth-mainnet-archive
    min_cpu: 32
    min_memory_mb: 32678
    min_disk_gb: 2000
    sku_code: BASE-OPRETH-MA
    archive_pointers:
      - pointer: !store_key base-op-reth-mainnet-archive-v1
firewall_config:
  default_in: deny
  default_out: allow
  rules:
    - key: Allow incoming ports TCP
      description: Allow TCP inbound traffic on port 80, 443
      protocol: tcp
      direction: in
      action: allow
      ips:
        - ip: 0.0.0.0/0
          name: allow all traffic
      ports:
        - port: 80
          name: HTTP port
        - port: 443
          name: HTTPS port
    - key: Allow P2P ports TCP/UDP
      description: Allow inbound traffic on P2P ports
      protocol: both
      direction: in
      action: allow
      ips:
        - ip: 0.0.0.0/0
          name: allow all traffic
      ports:
        - port: 30303
          name: P2P port
properties: []
